import type { NextPage } from "next";
import { Fragment } from "react";

const Home: NextPage = () => {
  return (
    <Fragment>
      <h1 className="text-3xl font-bold underline text-center h-screen flex items-center justify-center">
        I am arcms .. stay patient.. development is in progress now!
      </h1>
    </Fragment>
  );
};

export default Home;
